/**
 * Q1.7: 변수 이름 규칙
 *
 * - 잘못된 변수 이름을 선언하려고 하면 에러가 발생합니다.
 * - 아래 주어진 변수 이름들 중 올바른 것만 선언하고, 잘못된 이름들은 주석으로 표시하세요.
 */

let 1stVariable;  // 에러: 숫자로 시작하는 변수 이름
let _underscore;  // 올바른 변수 이름
let $dollarSign;  // 올바른 변수 이름
let var;          // 에러: 예약어 사용
let camelCase;    // 올바른 변수 이름

// TODO: 올바른 변수 이름만 활성화하고 console.log로 값을 출력하세요.

export {};