/**
 * Q8.8: getDataWithFallback 함수
 *
 * - getDataWithFallback(primary, fallback)를 작성하세요.
 * - primary()를 호출하여 반환된 Promise를 처리합니다.
 * - primary()가 실패하면 fallback()을 호출하여 반환된 Promise 값을 resolve합니다.
 *
 * @param {() => Promise<any>} primary
 * @param {() => Promise<any>} fallback
 * @returns {Promise<any>}
 */
function getDataWithFallback() {}

// export를 수정하지 마세요.
export { getDataWithFallback };
