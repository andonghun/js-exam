/**
 * Q8.9: parallelRequests 함수
 *
 * - parallelRequests(promise1, promise2)를 작성하세요.
 * - 두 개의 Promise를 병렬로 실행하고, 결과를 배열로 반환합니다.
 * - Promise.all 사용 권장.
 *
 * @param {Promise<any>} promise1
 * @param {Promise<any>} promise2
 * @returns {Promise<[any, any]>}
 */
function parallelRequests() {}

// export를 수정하지 마세요.
export { parallelRequests };
