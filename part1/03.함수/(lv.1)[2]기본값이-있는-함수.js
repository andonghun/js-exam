/**
 * Q3.13: 기본값이 있는 함수
 *
 * - introduce 함수는 이름(name)과 나이(age)를 받아 자기소개 메시지를 반환합니다.
 * - name은 필수값이고, age는 기본값으로 "비공개"를 사용합니다.
 * - "안녕하세요! 저는 {name}, 나이는 {age}입니다." 형태의 문자열을 반환하세요.
 *
 * @param {string} name
 * @param {string} [age="비공개"]
 * @returns {string}
 */

function introduce(name, age = "비공개") {
  // return
}

export { introduce };
