/**
 * Q: 파일 경로 정규화
 *
 * - normalizePath 함수를 작성하세요.
 * - 입력된 파일 경로를 정규화하여 반환합니다.
 * - '..'는 상위 폴더, '.'는 현재 폴더를 의미합니다.
 * - 시작과 끝의 '/'는 유지하되, 중간의 연속된 '/'는 하나로 처리합니다.
 *
 * 힌트:
 * 1. 경로를 '/'로 분리하여 각 부분을 배열로 만듭니다.
 * 2. '.'와 '..'를 처리하면서 배열을 순회합니다.
 *    - '.'는 무시
 *    - '..'는 이전 경로를 제거
 * 3. 처리된 배열을 다시 '/'로 결합합니다.
 * 4. 시작과 끝의 '/' 처리에 주의하세요.
 *
 * 예시:
 *   normalizePath("/a/b/../c") => "/a/c"
 *   normalizePath("./a//b/./c/") => "a/b/c/"
 *   normalizePath("/a/../../b") => "/b"
 *
 * @param {string} path
 * @returns {string}
 */

export function normalizePath(path) {
  // TODO
}
